<article>
    <div>
        <a href="/post/{{post.id}}">{{post.title}}</a>
    </div>
    <div>
        <p>{{post.content}}</p>
    </div>
    <div>
        <a href="/post/{{post.id}}">{{post.comments.length}} comment(s)</a>
    </div>
</article>